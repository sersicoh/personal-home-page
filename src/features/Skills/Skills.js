import { Container } from "../../common/Container/Container";
import { theme } from "../../theme";

export const Skills = () => (
  <Container backgroundColor={theme.colors.white} shadow padding>
    dupa
  </Container>
);
